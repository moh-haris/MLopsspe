---
# SecureNOC — Host Provisioning Only
# Usage: ansible-playbook setup.yml
# This playbook only runs the setup role (swap, Docker, user groups).
# For full deployment, use site.yml or deploy.yml.

- name: SecureNOC Host Provisioning
  hosts: localhost
  connection: local
  become: yes
  roles:
    - setup